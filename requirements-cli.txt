# Core dependencies for CLI usage (extract.py)
beautifulsoup4==4.14.2
requests==2.32.5
lxml==6.0.2
playwright==1.55.0
tqdm==4.67.1

# Image processing - allows latest Pillow (not constrained by Streamlit)
# Enables: optimization, format conversion, validation, EXIF data
pillow>=12.0.0
